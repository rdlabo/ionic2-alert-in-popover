# ionic2-alert-in-popover
This repository is revive that AlertController don't work in PopoverController.

## start
    $ npm install -g ionic
    $ cd ionic2-alert-in-popover
    $ npm i
    $ ionic serve
